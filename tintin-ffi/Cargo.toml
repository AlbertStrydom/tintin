[package]
name = "tintin-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "C-compatible FFI layer for TinTin's Rust core — used by iOS SwiftUI client"

[lib]
crate-type = ["staticlib", "cdylib"]
name = "tintin_core"

[dependencies]
tintin-core = { path = "../tintin-core" }
serde_json.workspace = true
